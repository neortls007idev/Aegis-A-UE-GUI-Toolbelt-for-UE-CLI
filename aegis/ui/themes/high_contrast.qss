QWidget { background: #000; color: #fff; }
QMenuBar, QMenu { background: #000; color:#fff; }
QDockWidget::title { background: #000; padding: 6px; color:#0ff; }
QTabBar::tab:selected { font-weight: 700; color:#0ff; }
QStatusBar { background: #000; color:#0ff; }
